{% set name = "pyobjcryst" %}
{% set version = "2026.1.0" %}
{% set sha256 = "0ca642db5d534bf24efce9a9f6d7c2e45026f5b0691fecb7fe8e69118535786e" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv
  skip: true  # [py<311]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ stdlib("c") }}
    - python                              # [build_platform != target_platform]
    - cross-python_{{ target_platform }}  # [build_platform != target_platform]
    - numpy                               # [build_platform != target_platform]
  host:
    - python
    - pip
    - setuptools
    - setuptools-git-versioning
    - numpy
    - libobjcryst 2026.2.0
    - libboost-python-devel
  run:
    - python
    - setuptools

test:
  source_files:
    - tests
  requires:
    - pip
    - pytest
  commands:
    - pip check
    - pytest

about:
  home: https://github.com/diffpy/pyobjcryst
  license: LicenseRef-diffpy
  license_file: LICENSE.rst
  summary: 'Open source (the license is 100% compatible with bsd-3 clause)
    Python bindings to the ObjCryst++ crystallographic library.'
  doc_url: https://pyobjcryst.readthedocs.io/
  dev_url: https://github.com/diffpy/pyobjcryst

extra:
  recipe-maintainers:
    - Tieqiong
    - vincefn
    - pavoljuhas
    - st3107
    - sbillinge
